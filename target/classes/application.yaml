server:
   port: 8090
   servlet:
      context-path: /BootCMS
spring:
   datasource:
      url: jdbc:mysql://localhost:3306/bootcms?characterEncoding=UTF-8&useUnicode=true&useSSL=false
      username: kemao_2
      password: 1234
      driver-class-name: com.mysql.jdbc.Driver
mybatis:
   mapper-locations: classpath:mapper/*.xml
   type-aliases-package: swim.gdkm.poji